/**
 * Punto de entrada del dominio de MutaDiff.
 * Los parsers, el modelo y el motor de comparación se exportan aquí
 * a partir de T-011. De momento solo expone la versión del paquete.
 */
export const version = '0.0.0';
